<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFApp"
        xmlns:Controller="clr-namespace:Controller;assembly=Controller" x:Class="WPFApp.WindowCompetitionInfo"
        mc:Ignorable="d"
        Title="Competition Info" Height="450" Width="675" ResizeMode="NoResize" Closing="Window_Closing">

    <Window.DataContext>
        <Controller:DataContext_CompetitionInfo/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <DataGrid ItemsSource="{Binding DriverData}" AutoGenerateColumns="False" CanUserReorderColumns="False" CanUserSortColumns="False" CanUserResizeColumns="False" Focusable="False" IsReadOnly="False" HorizontalAlignment="Left" Margin="160,51,0,10">
            <DataGrid.Columns>

                <DataGridTemplateColumn Header="Pictures">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding ImagePath}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Points" Binding="{Binding Points}"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
            </DataGrid.Columns>
        </DataGrid>

        <DataGrid Margin="400,51,0,10" HorizontalAlignment="Left" Width="142" ItemsSource="{Binding CompetitionData}" AutoGenerateColumns="False" CanUserReorderColumns="False" CanUserSortColumns="False" CanUserResizeColumns="False" Focusable="False" IsReadOnly="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Label Content="Tracks left in race" HorizontalAlignment="Left" Margin="400,20,0,0" VerticalAlignment="Top" Width="142"/>
        <Label Content="Leaderboard" HorizontalAlignment="Left" Margin="160,20,0,0" VerticalAlignment="Top" Width="142"/>
    </Grid>
</Window>
